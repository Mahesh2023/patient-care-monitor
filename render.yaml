services:
  - type: web
    name: patient-care-monitor
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend:app --host 0.0.0.0 --port ${PORT:-8000}"
    healthCheckPath: /api/health
