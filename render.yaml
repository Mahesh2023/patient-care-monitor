services:
  - type: web
    name: patient-care-monitor
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "10000"
    buildCommand: "echo 'Force rebuild'"
